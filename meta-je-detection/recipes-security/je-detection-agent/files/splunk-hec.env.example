# Copy to /etc/je-detection/splunk-hec.env on the target (never bake a
# real token into the image/recipe) and fill in real values, then:
#   systemctl restart je-detection-fluentbit   # or the sysvinit equivalent
JE_SPLUNK_HEC_HOST=
JE_SPLUNK_HEC_PORT=8088
JE_SPLUNK_HEC_TOKEN=
